version: '3.8'

services:
  postgres:
    container_name: apilibrary
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 
      POSTGRES_DB: apilibrary